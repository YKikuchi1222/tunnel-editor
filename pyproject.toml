[project]
name = "tunnel-editer"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv]
package = false

[dependency-groups]
dev = [
  "pytest>=8,<9",
  "shellcheck-py>=0.10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
